import styles from "./Rules.module.scss";

const Rules = () => {
  return (
    <section id="rules" className={styles.rules}>
      <h2>Rules &amp; FAQ</h2>
    </section>
  );
};

export default Rules;
